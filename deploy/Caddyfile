# Public REST only. MCP stays on the compose network (api:8000).
# HTTPS: terminate TLS at Cloudflare/nginx or add a hostname site block later.
:8003 {
	@mcp path /mcp /mcp/*
	respond @mcp 404
	reverse_proxy api:8000
}
